import fs from "fs";

class CartManager {
	constructor(pathFile) {
		this.pathFile = pathFile;
	}

    //addCart:  Añadir un carrito nuevo solamente insertandose con Id y el array de productos vacio

    //getCartById: Leer todo el archivo, encontrar el carrito a traves del id y devolver los productos que tiene adentro

    //addProductInCartById: Añadir un producto al carrito especificamente de un id de producto y especificamente una cantidad

}

export default CartManager;
